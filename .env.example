# =============================================================================
# Resume Tailor — copy to .env and fill in secrets
#   cp .env.example .env
# =============================================================================

OPENAI_API_KEY=

# API server (Docker / production). Defaults match docker-compose.
# HOST=0.0.0.0
# PORT=8765
# Comma-separated browser origins, or * (wildcard disables credentialed CORS).
# CORS_ORIGINS=https://app.example.com
# Absolute repo root (input/, web/, output/). Docker image sets /app; leave unset for local uv run from repo.
# RESUME_TAILOR_PROJECT_ROOT=/app

# Plain-text inputs (required)
RESUME_TEXT_PATH=input/resume.txt
JOB_DESCRIPTION_TEXT_PATH=input/job_description.txt

# Reference PDF for layout (agents see path in prompts; file ships as input/resume_template.pdf)
TEMPLATE_PDF_PATH=input/resume_template.pdf

OUTPUT_DIR=output

# Score loop: re-run full crew until both minimums are met or max rounds (defaults: 8, 80, 6).
# MIN_RECRUITER_SCORE=8
# MIN_ATS_SCORE=80
# MAX_CREW_SCORE_ROUNDS=6

# Legacy aliases still supported in main.py:
# RESUME_PATH=input/resume.txt
# JOB_DESCRIPTION_PATH=input/job_description.txt
